<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="12dp"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?attr/novaTextTitle"
            android:textSize="16sp"/>

        <!--
            Подпись у тех приложений, которые уходят мимо туннеля всегда.
            Без неё галочка у банка выглядела бы как то, что она решает, — а
            решает её закрытый список, а не пользователь.
        -->
        <TextView
            android:id="@+id/tv_app_direct"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:letterSpacing="0.05"
            android:text="direct"
            android:textColor="#FFD166"
            android:textSize="11sp"
            android:textStyle="bold"
            android:visibility="gone"/>
    </LinearLayout>

    <CheckBox
        android:id="@+id/cb_select"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:buttonTint="?attr/novaAccent"/>

</LinearLayout>
